<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.benew.filmbible.mapper.UserMapper">
    <resultMap id="userMap" type="UserDto">
        <id column="fb_user_idx" property="idx" />
        <result column="fb_user_id" property="userId"/>
        <result column="fb_user_pwd" property="userPwd" />
        <result column="fb_user_name" property="userName" />
        <result column="fb_user_reg_date" property="regDate" />
    </resultMap>

    <select id="findUserByIdx" resultMap="userMap">
        select
            *
        from FB_USER
        where fb_user_idx = #{userIdx}
    </select>

    <select id="findUserById" resultMap="userMap">
        select
            *
        from FB_USER
        where fb_user_id = #{userId}
    </select>
</mapper>
